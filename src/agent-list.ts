/**
 * @deprecated Import from `./list-compact.js` — kept for existing tests.
 */
export {
  compactAgentsListResult,
  COMPACT_AGENT_KEYS,
  type ListMode as AgentListMode,
} from './list-compact.js';
